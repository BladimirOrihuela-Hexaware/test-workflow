name: Update QA Build Version

on:
  push:
    branches:    
      - 'testing/develop'

jobs:
  UPDATE_QA_BUILD:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Setup git user
        uses: fregante/setup-git-user@v1

      - name: move
        run: |
          cd src/main/resources
     
      - name: execute file
        run: |
          VERSION=$(cat src/main/resources/qaBuild.txt)
          ((VERSION++))
          echo $VERSION > src/main/resources/qaBuild.txt
          git add .
          git commit -am "⬆️ Increment QA Build to $VERSION"
          git push
